{
    "scripts": {
        "build": "browserify index.js -t babelify --outfile index.dist.js",
        "watch": "watchify index.js -t babelify --outfile index.dist.js"
    },
    "devDependencies": {
        "babel": "^5.4",
        "babelify": "^6.1",
        "browserify": "^10.2",
        "watchify": "^3.2"
    }
}
